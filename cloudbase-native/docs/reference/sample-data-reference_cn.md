# 样例数据说明

这份文档用于说明 `users`、`tasks`、`task_archives` 的样例结构，方便调试、联调和补字段时对照。

## 建议用途

- 检查新增字段是否缺失
- 验证云函数返回结构
- 对照 archive 快照结构

## 使用建议

当模型字段发生变化时，应同步更新：

- `architecture/data-model_cn.md`
- 本文档的样例结构
- 相关云函数的读写逻辑
